# 正向 WebSocket 连接（Forward WebSocket）
# 用于连接已在局域网/本机运行的 Lagrange 正向 WS 服务
LAGRANGE_WS_TYPE=forward-websocket
LAGRANGE_WS_HOST=127.0.0.1
LAGRANGE_WS_PORT=8080
LAGRANGE_WS_ACCESS_TOKEN=your-access-token

# 反向 WebSocket 时使用（可选）
# LAGRANGE_WS_TYPE=backward-websocket
# LAGRANGE_WS_PATH=/onebot/v11/ws

# 数据库目录（仅在不使用 configPath 时作为 LAGRANGE_CORE_HOME 的 fallback）
# LAGRANGE_CORE_HOME=
# AUTHORIZED_USERS=111111,2222222,3333333